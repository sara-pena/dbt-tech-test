
version: 2

models:
  - name: dim_chargebacks
    description: |
      Table containing all the chargebacks
    columns:
      - name: chargeback_id
        description: Unique identifier for each chargeback
        data_tests:
          - unique
      - name: cvv_provided
        description: Boolean to tell if the in the related payment, the cvv was provided